# S.A.M. agent worker. The --mock loop and --bench harness need only httpx/dotenv (see below);
# the real LiveKit pipeline needs the agents stack.

# Exact versions resolved in the verified production-compatible worker environment.
livekit-agents==1.6.10              # TurnHandlingOptions + FallbackAdapter
livekit-plugins-elevenlabs==1.6.2   # ElevenLabs streaming TTS (our key)
livekit-plugins-openai==1.4.5       # OpenAI-compatible Groq/Cerebras clients
livekit-plugins-silero==1.4.5       # VAD
livekit-plugins-deepgram==1.6.10    # Direct Nova-3 + Flux STTv2/eot_timeout_ms support
livekit-plugins-turn-detector==1.6.10
livekit-api==1.2.0
livekit==1.1.14
livekit-protocol==1.1.22
livekit-blingfire==1.1.0
livekit-local-inference==0.2.6
# Default STT = LiveKit Inference (deepgram/nova-3 string model). Set DEEPGRAM_API_KEY + SAM_STT=deepgram to use direct.

# Token server (only needed for a custom client; the Agents Playground does not require it)
fastapi>=0.115
uvicorn>=0.32

# Brain/tools HTTP + bench harness
httpx>=0.27
websockets>=13
python-dotenv>=1.0

# Owner voice verification (Tier-T gate). On-device speaker recognition; inert unless
# PICOVOICE_ACCESS_KEY + SAM_OWNER_VOICEPRINT are set.
pveagle>=1.0
