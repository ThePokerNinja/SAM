"""S.A.M. agent worker — Samuel's realtime brain/voice runtime.

Samuel is the customer-facing agent; Hermes (internal "Charles") is the brain.
The --mock loop runs with the stdlib only; the real LiveKit pipeline is wired in Phase 5.
"""

__version__ = "0.0.1"
